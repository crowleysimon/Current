package com.crowleysimon.current.ui

/*
interface Routing {
    fun routeToWithBundle(navId: Int, bundle: Bundle)
}

class RouterFragment: Fragment(), Routing {

    override fun routeToWithBundle(navId: Int, bundle: Bundle) {
        findNavController().navigate(navId, bundle)
    }

    init {
        retainInstance = true
    }

    internal var isStarted: Boolean = false

    override fun onStart() {
        super.onStart()
        isStarted = true
    }

    override fun onStop() {
        super.onStop()
        isStarted = false
    }

    companion object {
        @JvmStatic
        fun fromActivity(activity: FragmentActivity): RouterFragment {
            return activity.supportFragmentManager.resolveFragment { RouterFragment() }
        }
    }
}*/
